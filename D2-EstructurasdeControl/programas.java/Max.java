/*Este codigo0 es para contar el timpo en los pits del piloto max verstapen sila parada 
es de 1.59 seg se considera una vuelta rapida y siendo 3.0 seg la mas lenta  */

import javax.swing.JOptionPane;

public class Max {
    public static void main(String[] args){
        // Declarear variables 
        double fastpit =0.0, slowpit=0.0;
        // entrada
        fastpit= Double.parseDouble(JOptionPane.showInputDialog("introduce el tiempo en pits " ));
        //operacion
        if (fastpit>=1.59){
            fastpit= Double.parseDouble(JOptionPane.showInputDialog("fue una vuelta muy rapida" ));
            if (numKilos >= 0.0 && numKilos<=2.0){
                total = subTotal;
                porcentaje = "0.0%";
                descuento = 0.0;
    }
    
}
